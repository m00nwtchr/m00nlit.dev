---
import { t, changeLanguage } from 'i18next';
import i18nConfig from '../../../astro-i18next.config.ts';

import Index from '../index.astro';

export function getStaticPaths() {
	return i18nConfig.locales.splice(1).map((lang) => ({
		params: {
			lang,
		},
	}));
}
---

<Index />
