---
import { locales, extractLocaleFromUrl, setLocale } from '../../paraglide/runtime.js';
import Index from '../index.astro';

export function getStaticPaths() {
	return locales.map((lang) => ({
		params: { lang },
	}));
}
---

<Index />
